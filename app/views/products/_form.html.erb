<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, 'error') %> prohibited this product from being saved:</h2>
      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, size: 60, :class => 'input_field' %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, rows: 10, cols: 59, :class => 'input_field' %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url, size: 60, :class => 'input_field' %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price, :class => 'input_field' %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'button_primary' %>
  </div>
<% end %>
